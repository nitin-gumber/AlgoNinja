export const userRoleEnum = {
  ADMIN: 'admin',
  USER: 'user',
};

export const availableUserRoles = Object.values(userRoleEnum);

export const difficultyEnum = {
  EASY: 'easy',
  MEDIUM: 'medium',
  HARD: 'hard',
};

export const availableDifficulties = Object.values(difficultyEnum);

export const planEnum = {
  FREE: 'free',
  PRO: 'pro',
  PREMIUM: 'premium',
};

export const plans = Object.values(planEnum);
